---
phase: 04-queue-profiles
plan: 06
type: execute
wave: 5
depends_on:
  - 04-03
  - 04-05
files_modified: []
autonomous: false
requirements:
  - QUEUE-01
  - QUEUE-02
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - PROF-05

must_haves:
  truths:
    - "All Phase 4 queue and profile requirements are exercised in the running application"
    - "Users can verify queue control and profile safety behaviors without losing their current workflow context"
    - "No regressions appear in existing Downloader, Checker, global search, settings, or logs flows"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for the complete Phase 4 queue and profile workflow set.

Purpose: Queue state transitions, diff previews, and undo affordances have meaningful UX risk. This checkpoint validates the end-to-end behavior in the running app after all implementation plans land.

Output: explicit user approval or a concrete list of issues to fix.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-queue-profiles/04-CONTEXT.md
@.planning/phases/04-queue-profiles/04-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Phase 4 queue and profile workflows end to end</name>
  <action>Launch the app after Plans 04-01 through 04-05 are complete, run the verification script below against the live UI, and pause for explicit user approval or defect details before closing the phase.</action>
  <what-built>
All Phase 4 queue and profile features integrated:
- Shared queue controller, header queue badge, drawer, and inline queue strips
- Downloader queue execution with pause, resume, cancel, inspect, retry, and skip recovery
- Checker queue execution for updates plus installed-mod enable and disable controls backed by `mod-list.json`
- Profile library with save-current flow and curated presets (`Vanilla+`, `QoL`, `Logistics and Rail`)
- Diff preview dialog with exact add/remove/enable/disable/download totals and explicit confirm
- Queue-backed profile apply with linked missing-mod downloads and one-click Undo Restore
  </what-built>
  <how-to-verify>
Start the app: `python -m factorio_mod_manager.main`

**QUEUE-01 — shared queue controls and state transitions**
1. Confirm the header utility zone shows a queue badge between global search and settings
2. Click the badge — a right-edge queue drawer opens without changing the current page
3. From Downloader, queue a download and confirm the badge count increments and the Downloader inline queue strip appears below `Download Mods`
4. From Checker, queue one or more updates and confirm they appear in the same shared drawer with source labels `Downloader` or `Checker`
5. Reorder queued items from the drawer and confirm only queued items move while the running item stays pinned
6. Pause a running queue item, then resume it; confirm visible states change to `Paused` then `Running`
7. Cancel a queued or running item and confirm it lands in `Canceled` with inspect access

**QUEUE-02 — failure recovery**
1. Force or simulate a failed download or update (for example by using an invalid mod name or disconnecting network mid-run)
2. Confirm the failed item remains visible and actionable in the drawer
3. Use `Inspect Details` and confirm a readable failure explanation appears
4. Use `Retry Failed Item` and confirm the item re-enters queue processing
5. Use `Skip Failed Item` and confirm later queued work continues rather than stopping automatically

**PROF-01 and PROF-02 — profile save and presets**
1. Open Checker and click the `Profiles` entry point in the header
2. Save the current mod state as a named profile and confirm it appears in the library
3. Open starter presets and confirm the available families are exactly `Vanilla+`, `QoL`, and `Logistics and Rail`

**PROF-03 — diff preview and explicit confirm**
1. Choose `Apply Profile` for a saved profile or preset
2. Confirm a dedicated diff dialog opens before any changes start
3. Confirm the left summary shows exact `Add`, `Remove`, `Enable`, `Disable`, and `Download` counts
4. Filter the diff by action type and confirm totals remain unchanged
5. Close the dialog with `Esc` or the window close button and confirm local state remains unchanged
6. Reopen it and use `Confirm Apply`; only then should queue work begin

**PROF-04 — undo restore**
1. After a successful profile apply, confirm a success toast offers `Undo Restore`
2. Open the queue drawer and confirm the completed profile-apply item also offers `Undo Restore`
3. Trigger undo from one of those entry points and confirm the pre-apply enabled-state snapshot is restored
4. Start a new profile apply and confirm the previous undo affordance disappears or becomes invalid

**PROF-05 — installed-mod enable or disable**
1. In Checker, confirm enabled-state control is separate from the selection checkbox
2. Disable a mod and confirm the row remains visible with disabled treatment
3. Verify the ZIP still exists on disk and only `mod-list.json` changed
4. Re-enable the same mod and confirm it becomes active immediately without a download flow

**Regression checks**
1. Global search still opens from the header and returns results
2. Settings still opens from the header/settings nav and theme switching still works
3. Logs page still renders and can be cleared
4. App exits cleanly with no uncaught exceptions in the terminal
  </how-to-verify>
  <verify>
    <automated>python -m compileall factorio_mod_manager</automated>
  </verify>
  <done>User either approves all seven Phase 4 requirements in the running app or returns a concrete issue list keyed to the failing requirement.</done>
  <resume-signal>Type "approved" when all checks pass, or describe the failing requirement and symptom.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual tester | Human validating runtime behavior rather than code artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 | n/a | Verification checkpoint | accept | This plan adds no code; implementation threats are handled in Plans 01 through 05 |
</threat_model>

<verification>
User confirms all seven Phase 4 requirements in the running application.
</verification>

<success_criteria>
- The user verifies QUEUE-01, QUEUE-02, PROF-01, PROF-02, PROF-03, PROF-04, and PROF-05 end to end
- Queue and profile features work without forcing navigation to a dedicated queue page
- No regressions are reported in existing shell, Downloader, Checker, Settings, Search, or Logs flows
</success_criteria>

<output>
After completion, create `.planning/phases/04-queue-profiles/04-06-SUMMARY.md`
</output>