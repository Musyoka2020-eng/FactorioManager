---
phase: 04-queue-profiles
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - factorio_mod_manager/ui/queue_controller.py
  - factorio_mod_manager/ui/queue_drawer.py
  - factorio_mod_manager/ui/queue_strip.py
  - factorio_mod_manager/ui/main_window.py
  - factorio_mod_manager/ui/styles/tokens.py
  - factorio_mod_manager/ui/styles/dark_theme.qss
  - factorio_mod_manager/ui/styles/light_theme.qss
  - tests/ui/test_queue_controller.py
autonomous: true
requirements:
  - QUEUE-01
  - QUEUE-02

must_haves:
  truths:
    - "A global queue badge is visible from anywhere in the shell and opens a non-modal drawer"
    - "One shared controller owns pause, resume, reorder, cancel, retry, skip, inspect, and clear-completed transitions"
    - "Reusable inline queue strips can show the same controller state inside Downloader and Checker without adding a dedicated queue page"
  artifacts:
    - path: factorio_mod_manager/ui/queue_controller.py
      provides: QObject queue state machine and signals
    - path: factorio_mod_manager/ui/queue_drawer.py
      provides: global queue drawer with actionable item cards
    - path: factorio_mod_manager/ui/main_window.py
      provides: header queue badge and drawer host wiring
  key_links:
    - from: factorio_mod_manager/ui/main_window.py
      to: factorio_mod_manager/ui/queue_drawer.py
      via: badge trigger toggles drawer visibility
      pattern: Open Queue
    - from: factorio_mod_manager/ui/queue_controller.py
      to: factorio_mod_manager/ui/queue_strip.py
      via: shared signals and snapshot rendering
      pattern: queue_changed
---

<objective>
Build the shared queue controller and shell-level queue surfaces that every workflow-specific Phase 4 plan plugs into.

Purpose: Queue behavior is global by decision. This plan establishes the single source of queue truth, the queue drawer, and the always-visible header trigger before Downloader, Checker, or profile apply flows try to enqueue work.

Output: reusable `QueueController`, `QueueDrawer`, and `QueueStrip` widgets plus `MainWindow` queue badge wiring and supporting QSS tokens/selectors.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-queue-profiles/04-CONTEXT.md
@.planning/phases/04-queue-profiles/04-UI-SPEC.md
@.planning/phases/02-fluent-shell-ux/02-CONTEXT.md
@.planning/phases/03-search-filter-settings/03-CONTEXT.md
@factorio_mod_manager/ui/main_window.py
@factorio_mod_manager/ui/styles/tokens.py
@factorio_mod_manager/ui/styles/dark_theme.qss
@factorio_mod_manager/ui/styles/light_theme.qss

<interfaces>
From `factorio_mod_manager/ui/main_window.py`:

```python
self._global_search_bar = GlobalSearchBar(self, parent=header_widget)
settings_btn.clicked.connect(lambda: self._navigate_to_page(3))
```

Phase 4 queue badge must be inserted between those two utility controls without adding a new left-rail destination.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the shared queue controller state machine with tests</name>
  <files>factorio_mod_manager/ui/queue_controller.py, tests/ui/test_queue_controller.py</files>
  <behavior>
    - Test 1: queued items can move up and down, while running items remain pinned and cannot be reordered
    - Test 2: pause, resume, cancel, retry, skip, and clear-completed transitions only allow legal state changes and preserve continue-on-failure semantics
    - Test 3: badge counts include queued, running, paused, and failed-awaiting-action items but exclude completed and canceled items after retention cleanup
  </behavior>
  <action>Create a Qt `QObject` controller that owns ordered `QueueOperation` instances from Plan 01 and emits signals for queue snapshots, badge counts, drawer-open requests, and inspect requests. Expose methods to enqueue work, start the next runnable item, pause/resume/cancel/retry/skip items, clear completed items, and move queued items. Keep worker execution abstract in this plan: the controller manages state and action dispatch, but workflow-specific jobs are added by later plans. Preserve D-04 continue-on-failure as the default policy and keep failed items actionable per D-05.</action>
  <verify>
    <automated>pytest tests/ui/test_queue_controller.py -q</automated>
  </verify>
  <done>`tests/ui/test_queue_controller.py` passes and a reusable queue controller exists with typed signals and legal transition enforcement.</done>
</task>

<task type="auto">
  <name>Task 2: Build reusable queue drawer and inline strip widgets with Phase 4 styling</name>
  <files>factorio_mod_manager/ui/queue_drawer.py, factorio_mod_manager/ui/queue_strip.py, factorio_mod_manager/ui/styles/tokens.py, factorio_mod_manager/ui/styles/dark_theme.qss, factorio_mod_manager/ui/styles/light_theme.qss</files>
  <action>Create a non-modal right-edge `QueueDrawer` anchored below the shell header and a compact `QueueStrip` summary widget for in-page workflow surfaces. The drawer must render the summary strip, active queue list, failed items, and recently completed items in the UI-SPEC order; item cards must expose the correct state-specific actions for `queued`, `running`, `paused`, `failed`, `completed`, and `canceled`. Extend tokens and both QSS themes with Phase 4 spacing, badge, chip, drawer, card, and diff/list selectors using the approved 4/8/16/24/32/48/64 scale and the locked semantic colors. All queue labels must remain plain-text Qt widgets, not rich text.</action>
  <verify>
    <automated>python -m compileall factorio_mod_manager/ui</automated>
  </verify>
  <done>`QueueDrawer` and `QueueStrip` exist, import successfully, and both themes include the selectors needed for the queue badge, drawer, cards, chips, and inline strips.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the global queue badge and drawer host into MainWindow</name>
  <files>factorio_mod_manager/ui/main_window.py</files>
  <action>Instantiate one shared `QueueController` and `QueueDrawer` in `MainWindow`, then add a persistent queue badge trigger in the header utility zone between the global search bar and settings button per D-03. The trigger must show `0` when idle, accent counts for active work, and a failed-state dot when recovery is needed; its tooltip and accessible name/description must match the UI-SPEC copy. Expose the controller and an `open_queue_drawer()` helper so child pages can enqueue work or open the drawer without modifying the shell structure or adding a new nav rail page. Keep the existing left rail, page indices, and settings/search behavior unchanged.</action>
  <verify>
    <automated>python -c "from factorio_mod_manager.ui.main_window import MainWindow; print('main_window import ok')"</automated>
  </verify>
  <done>`MainWindow` owns a shared queue controller, the header shows an always-visible queue badge trigger, and opening the queue drawer never navigates away from the current page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Queue operation metadata → shell UI | Operation titles, source labels, and inspect text cross into persistent UI surfaces |
| Page actions → shared controller | Multiple workflow surfaces can mutate global queue state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | Queue card text rendering | mitigate | Render all labels with Qt plain-text widgets and avoid `setTextFormat(RichText)` for queue content |
| T-04-05 | Denial of service | Repeated queue events flooding the shell | mitigate | Keep one shared controller, bound completed-history retention, and explicit `clear_completed` cleanup rather than unbounded item growth |
| T-04-06 | Elevation of privilege | Page-local actions mutating unrelated queue items | accept | Desktop-local app has one user context; constrain mutations to controller APIs that validate legal state transitions |
</threat_model>

<verification>
Run `pytest tests/ui/test_queue_controller.py -q` and `python -m compileall factorio_mod_manager/ui`.
</verification>

<success_criteria>
- `QueueController`, `QueueDrawer`, and `QueueStrip` exist and import cleanly
- The header queue badge is globally visible and opens a non-modal drawer instead of a new page
- Queue state transitions and badge counts are validated by automated tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-queue-profiles/04-02-SUMMARY.md`
</output>