---
phase: 00-pre-migration-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/00-pre-migration-cleanup/PARITY-CHECKLIST.md
autonomous: true
requirements:
  - PREP-05

must_haves:
  truths:
    - "A parity checklist document exists that Phase 1 executor can use to verify Qt migration completeness"
    - "Every observable user-facing behavior from the Tkinter app is listed with a verifiable pass/fail criterion"
  artifacts:
    - path: ".planning/phases/00-pre-migration-cleanup/PARITY-CHECKLIST.md"
      provides: "Behavioral parity checklist for Phase 1 verification"
      min_lines: 80
  key_links:
    - from: ".planning/codebase/FEATURES.md"
      to: "PARITY-CHECKLIST.md"
      via: "Every behavior in FEATURES.md maps to at least one checklist item"
---

<objective>
Produce a verifiable behavioral parity checklist from the FEATURES.md documentation.
Phase 1 (Qt migration) must pass every item on this checklist before it can be marked complete.

Purpose: Give the Phase 1 executor a concrete acceptance test list, not a vague "parity" goal.
Output: PARITY-CHECKLIST.md in the phase directory — grouped by feature area, each item checkable.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/FEATURES.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write PARITY-CHECKLIST.md from FEATURES.md</name>
  <files>.planning/phases/00-pre-migration-cleanup/PARITY-CHECKLIST.md</files>
  <read_first>
    - .planning/codebase/FEATURES.md — PRIMARY SOURCE. Every behavior listed here must
      appear in the checklist.
    - .planning/codebase/CONCERNS.md — note threading bugs that must NOT be replicated
      in Qt (they are bugs to leave behind, not behaviors to copy)
  </read_first>
  <action>
Create `.planning/phases/00-pre-migration-cleanup/PARITY-CHECKLIST.md` with the
following structure. Use FEATURES.md as the source of truth.

Each checklist item must be:
- [ ] checkable (a box to tick when verified in the Qt app)
- Specific enough to verify manually (not "works correctly" — say WHAT to do and WHAT to see)
- Derived from FEATURES.md section content

Sections to cover (minimum — add more if FEATURES.md has additional behaviors):

## Application Startup
- Window launches maximized with 1100×750 minimum size
- Header shows title "Factorio Mod Manager" with version
- Blue accent separator visible in header
- Three tabs visible: Downloader, Checker & Updates, Logs
- Status bar present at bottom
- Mods folder auto-populated from config.json on launch

## Tab 1 — Downloader
- Mod URL input field present with placeholder text
- Load Mod button present
- Mods Folder display with Browse button
- "Include optional dependencies" checkbox present
- Download button present
- Typing in URL field triggers debounced search after ~500ms (no search on each keystroke)
- Load Mod triggers full recursive dependency resolution and displays results
- Browse opens native OS folder picker; selection persists on next launch
- Download validates: empty URL shows error notification
- Download validates: no folder selected shows error notification  
- Download validates: offline shows error notification; Download button re-enables after (PREP-03 fix)
- Download progress bar visible and advances during download
- Progress console shows color-coded log lines (info=blue, success=green, error=red, warning=yellow)
- Per-mod sidebar shows one row per mod with status transitions: Preparing → Downloading → ✓/✗
- Download success shows green toast notification with mod count
- Download failure shows red toast notification with failed mod list

## Tab 2 — Checker & Updates
- Three-column layout: left sidebar, center mod list, right sidebar
- Left sidebar: mods folder picker + action buttons (Scan, Check Updates, Update Selected, Delete, Update All, Backups, Backup, View Details)
- Center: installed mods list with checkbox, name, status, version, author, downloads columns
- Right sidebar: statistics panel + search box + status filter buttons + sort radio buttons
- Logs panel spans full width at bottom
- Auto-scan triggers ~3 seconds after tab first becomes visible (only once)
- Scan button: fetches all *.zip mods and portal metadata; updates list and stats
- Check Updates: re-fetches portal if stale; shows update count in toast
- Update Selected: requires at least one checkbox selected; downloads updates
- Delete: shows persistent confirmation notification with action buttons before deleting
- Backup: copies selected zips to mods/backup/ subfolder
- Clean Backups: shows size + persistent confirmation before deleting backup/ folder
- View Details: only enabled with exactly 1 mod selected; opens scrollable popup
- Checkbox click selects/deselects row; Ctrl+click accumulates selection; selected rows highlight
- Search box filters mod list live on every keystroke (case-insensitive)
- Status filter buttons (All/Outdated/Up to Date/Selected) filter the list immediately
- Sort radio buttons (Name/Version/Downloads/Date) re-sort immediately
- Statistics panel shows: total, up to date, outdated, unknown/error, total downloads counts

## Tab 3 — Logs
- Full-width scrollable log display
- Color-coded: ERROR=red, WARNING=yellow, DEBUG=gray, INFO=gray
- Auto-scrolls to latest entry
- Clear button present and clears log display when clicked (PREP-04 fix)
- All operations from Downloader and Checker tabs also appear in Logs tab

## Notifications System
- Success toast: green, 4000ms default, appears then auto-dismisses
- Error toast: red, appears then auto-dismisses
- Warning toast: yellow, appears then auto-dismisses
- Persistent notification: does not dismiss until user acts (used for Delete and Clean Backups confirmations)
- Persistent notification includes action buttons (e.g., "Delete" + "Cancel")
- Multiple toasts stack vertically if triggered simultaneously

## Status Bar
- Status bar updates during active operations
- Working status: shows progress text (e.g., "Downloading: 2/5 mods (40%)")
- Success status: shows green completion message after operation ends
- Error status: shows red error message on failure

## Configuration Persistence
- mods_folder survives app restart
- download_optional checkbox state survives app restart

## Behaviors NOT to replicate (Tkinter bugs — leave behind in Qt)
- Direct widget mutation from background threads (Tkinter threading bug — Qt must use signals/slots)
- Bare except: / except Exception blocks that swallow errors silently

---

*Generated: 2026-04-10*
*Source: .planning/codebase/FEATURES.md*
*Used by: Phase 1 (Qt Platform Migration) to verify behavior parity before Phase 1 completion*
  </action>
  <verify>
    <automated>python -c "
import os
path = '.planning/phases/00-pre-migration-cleanup/PARITY-CHECKLIST.md'
assert os.path.exists(path), f'File not found: {path}'
content = open(path).read()
lines = content.splitlines()
assert len(lines) >= 60, f'Too short: {len(lines)} lines (expected >= 60)'
assert '- [ ]' in content, 'No checkboxes found'
checklist_items = content.count('- [ ]')
assert checklist_items >= 30, f'Only {checklist_items} checklist items (expected >= 30)'
print(f'PASS: {checklist_items} checklist items, {len(lines)} lines')
"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/00-pre-migration-cleanup/PARITY-CHECKLIST.md` exists
    - File contains at least 30 `- [ ]` checkbox items
    - File has sections for: Startup, Downloader, Checker, Logs, Notifications, Status Bar, Config, Bugs NOT to replicate
    - Each item is specific enough for manual verification (not "works correctly")
    - File notes PREP-03 (button re-enable) and PREP-04 (Clear button) as required behaviors in the Qt version
  </acceptance_criteria>
  <done>PARITY-CHECKLIST.md exists with >= 30 verifiable items covering all FEATURES.md behaviors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation only | No executable code, no trust boundaries in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-05 | Information Disclosure | PARITY-CHECKLIST.md | accept | Documentation file, no sensitive data. Low risk. |
</threat_model>

<verification>
```bash
# File exists with sufficient content
python -c "
path = '.planning/phases/00-pre-migration-cleanup/PARITY-CHECKLIST.md'
c = open(path).read()
items = c.count('- [ ]')
print(f'Checklist items: {items}')
assert items >= 30
print('PASS')
"
```
</verification>

<success_criteria>
- PARITY-CHECKLIST.md created in `.planning/phases/00-pre-migration-cleanup/`
- At least 30 checkbox items
- Covers all major behavior areas from FEATURES.md
- References both PREP-03 and PREP-04 fixes as required Qt behaviors
- Explicitly lists threading behavior as a bug NOT to replicate
</success_criteria>

<output>
After completion, create `.planning/phases/00-pre-migration-cleanup/00-03-SUMMARY.md`
</output>
