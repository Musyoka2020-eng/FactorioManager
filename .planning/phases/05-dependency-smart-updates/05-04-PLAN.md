---
phase: 05-dependency-smart-updates
plan: 04
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
files_modified: []
autonomous: false
requirements:
  - DEPS-01
  - DEPS-02
  - DEPS-03
  - UPDT-01
  - UPDT-02
  - UPDT-03

must_haves:
  truths:
    - "Read-only dependency graph opens in the Dependencies tab of the mod details dialog"
    - "Simplified / Full view toggle switches between direct-only and transitive dependency trees"
    - "All five dependency states are visible — Required, Optional, Incompatible, Expansion, Missing"
    - "Safe / Review / Risky classifications appear with 2-4 rationale bullets for each outdated mod"
    - "Queue Safe Updates button successfully queues only Safe-classified mods and they appear in the queue strip"
    - "Changelog with version delta highlighting opens in the Changelog tab of the mod details dialog"
  artifacts:
    - path: "factorio_mod_manager/core/dependency_graph.py"
      provides: "build_dep_graph function"
      contains: "def build_dep_graph"
    - path: "factorio_mod_manager/core/update_guidance.py"
      provides: "UpdateGuidanceClassifier"
      contains: "class UpdateGuidanceClassifier"
    - path: "factorio_mod_manager/ui/mod_details_dialog.py"
      provides: "QTabWidget with Dependencies and Changelog tabs"
      contains: "DependenciesWidget"
    - path: "factorio_mod_manager/ui/checker_tab.py"
      provides: "SmartUpdateStrip, Guidance column, Selected guidance panel"
      contains: "class SmartUpdateStrip"
  key_links:
    - from: "CheckerTab.mod_table"
      to: "ModDetailsDialog"
      via: "_on_view_details opens dialog with .exec()"
      pattern: "ModDetailsDialog.*exec"
    - from: "SmartUpdateStrip.queue_safe_requested"
      to: "QueueController"
      via: "_on_queue_safe_updates enqueues QueueOperation(kind=UPDATE)"
      pattern: "_on_queue_safe_updates"
---

<objective>
Human verification of all Phase 5 features: dependency graph, view toggle, dep state colours, guidance classifications with rationale, Queue Safe Updates, and changelog display.

Purpose: Confirms that Plans 01–03 delivered every user-visible outcome specified in CONTEXT.md decisions D-01 to D-18. Acts as the final UAT gate before the phase is closed.

Output: User confirmation that all 6 success criteria pass, plus any issues to feed into a gap-closure plan if needed.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-dependency-smart-updates/05-CONTEXT.md
@.planning/phases/05-dependency-smart-updates/05-UI-SPEC.md
@.planning/phases/05-dependency-smart-updates/05-02-SUMMARY.md
@.planning/phases/05-dependency-smart-updates/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and import sanity checks before handing off to user</name>
  <read_first>
    - .planning/phases/05-dependency-smart-updates/05-01-SUMMARY.md (confirm dependency_graph.py and update_guidance.py tests all pass)
    - .planning/phases/05-dependency-smart-updates/05-02-SUMMARY.md (confirm ModDetailsDialog tab structure)
    - .planning/phases/05-dependency-smart-updates/05-03-SUMMARY.md (confirm SmartUpdateStrip and guidance pipeline)
  </read_first>
  <action>
    Run the full import sanity check and test suite:

    ```bash
    python -c "
    from factorio_mod_manager.core.dependency_graph import DepType, DepState, DepNode, build_dep_graph
    from factorio_mod_manager.core.update_guidance import UpdateClassification, GuidanceResult, UpdateGuidanceClassifier
    from factorio_mod_manager.ui.mod_details_dialog import ModDetailsDialog
    from factorio_mod_manager.ui.checker_tab import CheckerTab, SmartUpdateStrip, ClassifyWorker
    from factorio_mod_manager.ui.checker_logic import CheckerLogic
    from factorio_mod_manager.ui.checker_presenter import CheckerPresenter
    from factorio_mod_manager.ui.filter_sort_bar import FilterSortBar
    print('All Phase 5 imports OK')
    "

    pytest tests/ -x -q
    ```

    If pytest finds failures, triage and fix before proceeding to the checkpoint. If only import errors, fix the specific missing import or wrong module path in the relevant file.

    Once tests pass, report the passing count in the SUMMARY.
  </action>
  <verify>
    <automated>pytest tests/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ -x -q` exits 0 with all tests passing
    - Import sanity check prints "All Phase 5 imports OK"
    - `grep -rn "setHtml" factorio_mod_manager/ui/mod_details_dialog.py` returns NO match (security: only setPlainText used for untrusted portal content)
  </acceptance_criteria>
  <done>Full test suite passes. All 7 Phase 5 modules import cleanly. No setHtml calls in mod_details_dialog.py.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Human UAT — verify all six Phase 5 success criteria</name>
  <what-built>
    Plans 01–03 delivered:
    - Plan 01: dependency_graph.py (build_dep_graph, DepType, DepNode, DepState) and update_guidance.py (UpdateGuidanceClassifier, GuidanceResult, UpdateClassification) with full test coverage
    - Plan 02: ModDetailsDialog refactored to QTabWidget (Overview / Dependencies / Changelog). Dependencies tab shows QTreeWidget dep graph with Simplified/Full toggle and colour-coded dep state chips. Changelog tab shows version entries with delta highlighting.
    - Plan 03: Checker intelligence layer — ClassifyWorker, SmartUpdateStrip, Guidance column in mod table, Queue Safe Updates button, Review/Risky confirmation dialog, Selected Update Guidance panel in right sidebar, guidance filter in filter bar, fixed _on_view_details to open ModDetailsDialog.
  </what-built>
  <how-to-verify>
    Launch the application:
    ```
    python -m factorio_mod_manager.main
    ```

    Then verify each criterion in order:

    **1. Dependency Graph opens (D-01, D-02, DEPS-01)**
    - Navigate to the Checker tab
    - Right-click any installed mod → View Details (or select mod, click View Details button in the right sidebar)
    - Click the "Dependencies" tab in the dialog
    - Expected: QTreeWidget shows the direct dependencies of the mod with colour-coded chips per dep state (Required, Optional, Incompatible, Expansion, Missing)

    **2. Simplified / Full view toggle (D-04, DEPS-02)**
    - With Dependencies tab open, look for the Simplified / Full toggle buttons above the tree
    - Click "Full" — expected: transitive dependencies appear as nested children
    - Click "Simplified" — expected: only direct dependencies shown
    - Verify toggle buttons are mutually exclusive (only one active at a time)

    **3. All 5 dependency states visible (D-03, DEPS-01)**
    For any mod with multiple dep types (e.g., base game + optional + incompatible), confirm:
    - Required dep = white / neutral chip
    - Optional dep = muted chip
    - Incompatible dep = red chip with "!" indicator
    - DLC/Expansion dep = distinct chip to differentiate from normal
    - Missing dep (installed mod that references a dep not installed) = amber / warning chip

    **4. Safe / Review / Risky classifications with rationale (D-11, D-12, UPDT-01)**
    - Run Check for Updates (let it complete)
    - Expected: Each outdated mod in the Checker table shows a chip in the Guidance column (col 4)
    - Select one outdated mod → look at the Selected Update Guidance panel (right sidebar)
    - Expected: Chip label (Safe / Review / Risky) + 2–4 rationale bullet points + dep delta summary
    - Click "View Details" in the guidance panel → dialog opens to Dependencies tab

    **5. Queue Safe Updates (D-15, UPDT-02)**
    - After Check for Updates, observe the SmartUpdateStrip between the filter bar and the mod table
    - Expected: Strip shows e.g. "3 Safe  1 Review  0 Risky" with coloured labels
    - Click "Queue Safe Updates" — expected: button is disabled until Safe count > 0; clicking queues the safe batch
    - Expected: queue strip shows the new operation with "(only Safe updates)" label or similar count
    - Verify that Review / Risky mods in the list were NOT queued by this button
    - Now manually select a mod that is Review or Risky, click "Update Selected"
    - Expected: _UpdateConfirmDialog appears with counts before queuing proceeds

    **6. Changelog with delta highlighting (D-17, UPDT-03)**
    - Select any outdated mod, click View Details
    - Click the "Changelog" tab
    - Expected: Latest version entry is highlighted / shown first; text diff between current installed version and latest version is visually distinguished
    - Confirm the content is plain text (no raw HTML tags rendered)
    - Confirm it shows at least the most recent changelog entry even if the mod has many versions

    ---
    Type "approved" if ALL 6 criteria pass.
    If any fail, describe which criterion failed and what you observed — a gap-closure plan will be created to address it.
  </how-to-verify>
  <resume-signal>
    Type "approved" to close the phase, or describe any failing criteria for gap-closure planning.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification only — no new code paths | This plan performs read-only verification; no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Verification | Security check — setPlainText enforcement | mitigate | Task 1 acceptance criteria include `grep -rn "setHtml"` returning zero results in mod_details_dialog.py; verifies no raw HTML injection path was introduced during Plan 02/03 implementation |
</threat_model>

<verification>
All Phase 5 imports pass without error.
`pytest tests/ -x -q` exits 0.
User confirms all 6 UAT criteria in checkpoint human-verify.
</verification>

<success_criteria>
- All pytest tests pass (exit 0)
- Import sanity check passes for all 7 new/modified modules
- User confirms all 6 Phase 5 UAT criteria:
  1. Dep graph opens in Dependencies tab with colour-coded chips
  2. Simplified / Full toggle switches tree depth
  3. All 5 dep states visible (Required, Optional, Incompatible, Expansion, Missing)
  4. Safe / Review / Risky chips in Guidance column with rationale in right sidebar
  5. Queue Safe Updates batches only Safe mods; Review / Risky selections show confirmation dialog
  6. Changelog tab shows version delta highlighting with plain text (no raw HTML)
</success_criteria>

<output>
After completion, create `.planning/phases/05-dependency-smart-updates/05-04-SUMMARY.md`
</output>
